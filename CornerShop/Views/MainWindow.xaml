<Window x:Class="CornerShop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CornerShop"
        mc:Ignorable="d"
        Title="CornerShop Login" Height="500" Width="800" WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize" FontFamily="Segoe UI" Background="#FFF7F7F7">

    <Window.Resources>

        <Style x:Key="ModernInputBox" TargetType="Control">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F9F9F9"/>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                CornerRadius="8" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#667EEA" Offset="0.0"/>
                <GradientStop Color="#764BA2" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Width="380" Height="450" 
                Background="White" 
                CornerRadius="15"
                VerticalAlignment="Center" HorizontalAlignment="Center">

            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="5" Opacity="0.3" Color="Black"/>
            </Border.Effect>

            <StackPanel Margin="30">

                <TextBlock Text="Welcome Back" 
                           FontSize="28" FontWeight="Bold" Foreground="#333333" 
                           HorizontalAlignment="Center" Margin="0,10,0,5"/>

                <TextBlock Text="Please login to your account" 
                           FontSize="14" Foreground="#888888" 
                           HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <TextBlock Text="Username" FontWeight="SemiBold" Foreground="#555555" Margin="0,0,0,5"/>
                <TextBox x:Name="UserBox" Style="{StaticResource ModernInputBox}" Margin="0,0,0,15"/>

                <TextBlock Text="Password" FontWeight="SemiBold" Foreground="#555555" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" Style="{StaticResource ModernInputBox}" Margin="0,0,0,30"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" 
                            Content="Login" 
                            Click="Login_Click"
                            Style="{StaticResource ModernButton}"
                            Background="#00B894"/>

                    <Button Grid.Column="2" 
                            Content="Register" 
                            Click="Register_Click"
                            Style="{StaticResource ModernButton}"
                            Background="#6C5CE7"/>
                </Grid>

            </StackPanel>
        </Border>


        <!--<Grid Margin="20">
        <Border CornerRadius="10" Background="White" Padding="30">
            <StackPanel VerticalAlignment="Center">

                <TextBlock Text="Welcome to Corner Shop"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#333"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

                
                <TextBox x:Name="UsernameBox"
                         Height="35"
                         Margin="0,0,0,15"
                         Padding="10,0"
                         FontSize="14"
                         BorderBrush="#CCC"
                         BorderThickness="1"
                         Background="#F0F0F0"
                         VerticalContentAlignment="Center"
                         Foreground="#666">
                    <TextBox.Text>
                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"/>
                    </TextBox.Text>
                    <TextBox.Tag>Username</TextBox.Tag>
                </TextBox>

                --><!-- Password --><!--
                <PasswordBox x:Name="PasswordBox"
                             Height="35"
                             Margin="0,0,0,20"
                             Padding="10,0"
                             FontSize="14"
                             BorderBrush="#CCC"
                             BorderThickness="1"
                             Background="#F0F0F0"
                             VerticalContentAlignment="Center"/>
                

                <Button Content="Login"
                        Height="40"
                        Margin="0,0,0,10"
                        FontWeight="Bold"
                        Foreground="White"
                        Background="#FF6C00"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="Login_Click"/>

                <Button Content="Register"
                        Height="35"
                        FontWeight="SemiBold"
                        Foreground="#FF6C00"
                        Background="Transparent"
                        BorderBrush="#FF6C00"
                        BorderThickness="1"
                        Cursor="Hand"
                        Click="Register_Click"/>
            </StackPanel>
        </Border>-->
    </Grid>
</Window>
